# Gabriel Automações — Site Portfólio

Site estático em HTML, CSS e JavaScript, pronto para GitHub Pages.

## Arquivos
- `index.html` — estrutura do site
- `styles.css` — identidade visual e responsividade
- `script.js` — interações e demos simuladas

## Publicar no GitHub Pages
1. Crie um repositório no GitHub.
2. Envie `index.html`, `styles.css` e `script.js` para a raiz do repositório.
3. Abra **Settings → Pages**.
4. Em **Build and deployment**, escolha **Deploy from a branch**.
5. Selecione a branch `main` e a pasta `/ (root)`.
6. Salve. O GitHub fornecerá a URL pública.

## Próxima etapa
Substituir as simulações do `script.js` por chamadas `fetch()` para webhooks específicos do n8n.
